*** WooCommerce Side Cart ***

2026.07.03 - version 3.3.4
 * Cleanup: remove temporary console debug tracing used to diagnose the side-cart remove/add-to-cart synchronization issue.
 * Fix: keep the session-first cart synchronization introduced in 3.3.3 for remove-item, quantity updates and external add-to-cart refreshes.

2026.07.03 - version 3.3.3
 * Fix: prefer the WooCommerce session/nonce over persisted Cart-Token during side-cart remove/update and after external add-to-cart events.
 * Fix: ignore internal block cart sync events emitted by the plugin itself to avoid duplicate refresh/open flows after remove-item mutations.

2026.07.03 - version 3.3.2
 * Debug: add temporary console logging to trace Store API cart token, cart hash, remove-item and external add-to-cart sync flows.
 * Fix: preserve safer auth-aware client state handling for Store API nonce/bootstrap.

2026.06.06 - version 3.3.1
 * Fix: removing the last cart item no longer restores stale items after refresh (Store API cart token handling).

2026.06.04 - version 3.3.0
 * Performance: hard-disable on Checkout (no hooks, no assets, no markup).
 * Performance: server-side footer markup is now minimal; items/totals render client-side only.
 * Performance: UI listeners bind on-demand (first interaction / first open).
 * Build: ships minified JS bundle.

2026.06.03 - version 3.2.0
 * Full Page Cache (guest): optional hydration on load via Store API to keep badge/count + hooksContext.cart in sync.

2026.06.03 - version 3.2.1
 * Cleanup: config parity fixes (ui.showItemLinks now respected, ui.disableUiListeners configurable, composite.summary configurable), removed legacy activation side-effect, removed unused template.

2026.06.03 - version 3.2.2
 * Fix: autoOpenOnAddToCart now supports WooCommerce single product AJAX (button.single_add_to_cart_button) via added_to_cart event.

2026.06.03 - version 3.2.3
 * Fix: autoOpenOnAddToCart now supports added_to_cart payloads that pass a DOM element as $button (not only jQuery objects).

2026.06.03 - version 3.2.4
 * Fix: autoOpenOnAddToCart now also opens when added_to_cart is triggered without passing $button/fragments arguments.

2026.06.03 - version 3.2.5
 * Fix: autoOpenOnAddToCart now supports added_to_cart emitted as a CustomEvent (payload in event.detail).

2015.04.22 - version 1.0.0
 * Initial Release.
